<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Existing path for sharing cached images -->
    <cache-path name="shared_images" path="images/"/>

    <!-- New path for sharing cached PDFs -->
    <cache-path name="shared_pdfs" path="shared_pdfs/"/>
</paths>
