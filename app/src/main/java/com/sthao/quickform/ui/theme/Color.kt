package com.sthao.quickform.ui.theme

import androidx.compose.ui.graphics.Color

// Defines the specific color values for your blue and light blue theme.
val Blue = Color(0xFF007BFF) // A strong, primary blue.
val LightBlue = Color(0xFF89CFF0) // A softer, secondary light blue.
val Grey = Color(0xFFB2B2B2) // A light grey for backgrounds to ensure contrast.
val White = Color(0xFFFFFFFF) // Pure white for surfaces like cards.
val Black = Color(0xFF000000) // Pure black for text to ensure readability.

// FAB UI colors
val FabColorNew = Color(0xFFE88862)
val FabColorSave = Color(0xFF9C79EA)
val FabColorView = Color(0xFF0AAD9F)

// Form colors
val PickupForm = Color(0xFF8CC989)
val DropOffForm = Color(0xFFEA9794)
val StationsForm = Color(0xFFADD8E6)
val SavedForms = Color(0xFFFDF2CD)
val FormsSelected = Color(0xFFCCC4EE)
val DarkGray50 = Color(0xFF5D5D5D)